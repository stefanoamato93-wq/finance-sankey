{
  "name": "finance-sankey-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only test harness for finance-sankey-web/index.html. Never referenced by the shipped single-file page.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "fast-check": "3.23.2",
    "vitest": "2.1.9"
  }
}
